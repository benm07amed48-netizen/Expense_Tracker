# Expense_Tracker
This python app is for managing personal expenses 

## CLI scaffold

Initial CLI structure has been created in `/expense_tracker` with:

- `expense_tracker/cli.py` (CLI parser + `main`)
- `expense_tracker/__main__.py` (`python -m expense_tracker` entrypoint)
- package placeholders for commands, models, and services
